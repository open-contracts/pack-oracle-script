This repo contains a script that packs an Open Contracts oracle.
It also hosts it via GitHub pages., such that it can be executed from the command line by running 
```
curl -L pack.opencontracts.io | bash
```
